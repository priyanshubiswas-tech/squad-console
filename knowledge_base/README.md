# knowledge_base (placeholder)

Not populated yet. Will hold one folder per team (`england/`, `france/`, `brazil/`, `argentina/`, `spain/`, `germany/`, `portugal/`) each with `tactics_notes.md` (private, full detail) and `public_scouting.md` (public), plus `shared/tactical_theory.md`. This folder is bind-mounted into containers (not baked into an image) so files can be edited live and re-embedded without a rebuild.
