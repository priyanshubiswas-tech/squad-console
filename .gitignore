# secrets
.env

# original planning docs - kept locally only, not published (full of
# pre-rebrand "Tactica" branding)
TACTICA_BUILD_SPEC*.md
Tactica_Architecture*.pdf

# python
__pycache__/
*.pyc
.venv/
*.egg-info/

# node / frontend (once it exists)
node_modules/
dist/
build/

# Generated chart PNGs live in the charts_data Docker volume (mounted at
# /app/charts inside the backend container), never on the host filesystem -
# so there's nothing to ignore here. backend/app/charts/ is real source code.

# ClickHouse volume backups (see README § Backing up and restoring ClickHouse data)
clickhouse_data_backup.tar.gz

# editor / OS
.DS_Store
.vscode/
.idea/
