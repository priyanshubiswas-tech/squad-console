# embedding_job (placeholder)

Not built yet. This will chunk and embed the markdown files in `knowledge_base/` into ChromaDB collections (`{team}_full`, `{team}_public`, `shared_theory`), tagged with `{team, doc_type, visibility}` metadata, for the LangGraph RAG pipeline to query.
